# ToLetHub Frontend Environment Variables
# In production, these will be read by your hosting or Docker setup.
# During local development, the development proxy (proxy.conf.json) forwards requests from /api to localhost:3000

NG_APP_API_URL=http://localhost:3000
